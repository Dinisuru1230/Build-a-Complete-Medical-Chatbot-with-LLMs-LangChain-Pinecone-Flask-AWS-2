
## steps
clone the repository

''''bash

git clone https://github.com/Dinisuru1230/Build-a-Complete-Medical-Chatbot-with-LLMs-LangChain-Pinecone-Flask-AWS-2.git

## step 01 create a conda environment after opening the repository
''''bash
conda create -n madibot python = 3.10 -y
''''bash
conda activate medibot
'''''
## step 2 - install the requirmens
pip install -r requirements.txt